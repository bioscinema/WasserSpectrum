% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/wasserstein_test_pairwise.r
\name{wasserstein_test_pairwise}
\alias{wasserstein_test_pairwise}
\title{Pairwise Permutation Wasserstein Tests (wrapper around `wasserstein_test`)}
\usage{
wasserstein_test_pairwise(
  df,
  feature_col,
  group_col,
  nperm = 1000,
  seed = 123,
  pair_sep = "-"
)
}
\arguments{
\item{df}{Data frame with the feature and group columns.}

\item{feature_col}{String; column name for the feature (e.g., diversity index).}

\item{group_col}{String; column name for the grouping variable (>= 2 unique values).}

\item{nperm}{Integer; number of permutations per pair (default 1000).}

\item{seed}{Integer; base seed (each pair uses `seed + i` for reproducibility).}

\item{pair_sep}{String; separator used in the pair label (default "-").}
}
\value{
A data.frame with columns:
\item{comparison}{Pair label like "group1-group2".}
\item{statistic}{Observed 1D Wasserstein distance for the pair.}
\item{p_value}{Right-tailed permutation p-value.}
}
\description{
Runs `wasserstein_test()` for all pairwise comparisons of the groups in `group_col`.
}
\examples{
\dontrun{
pair_res <- wasserstein_test_pairwise(df, "Shannon", "Group", nperm = 2000)
pair_res[order(pair_res$p_value), ]
}
}
