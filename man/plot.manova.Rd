% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot.manova.R
\name{plot.manova}
\alias{plot.manova}
\title{Plot Quantile-wise MANOVA Results}
\usage{
\method{plot}{manova}(
  manova_df,
  type = c("both", "F", "p"),
  p_cutoff = 0.05,
  line_color = "darkblue",
  ribbon_color = "lightblue",
  title = "Quantile-wise MANOVA",
  base_size = 14
)
}
\arguments{
\item{manova_df}{A data frame returned by \code{\link{spectrum_manova}}, containing columns
\code{"quantile"}, \code{"Fval"}, and \code{"pval"}.}

\item{type}{Type of plot to return: \code{"F"} for F-statistics only,
\code{"p"} for \(-\log_{10}(p)\)-values only, or \code{"both"} (default) for stacked plots.}

\item{p_cutoff}{Significance threshold to highlight in the \(-\log_{10}(p)\) plot (default is 0.05).}

\item{line_color}{Color for the line plots (default is \code{"darkblue"}).}

\item{ribbon_color}{Ignored in current implementation; reserved for future use.}

\item{title}{Title for the plot (default is \code{"Quantile-wise MANOVA"}).}

\item{base_size}{Base font size for ggplot theme (default is 14).}
}
\value{
A \code{ggplot} object. If \code{type = "both"}, returns a vertically stacked plot using \code{patchwork}.
}
\description{
Visualizes the output of \code{\link{spectrum_manova}} as a function of quantile level,
showing either the F-statistics, \(-\log_{10}(p)\)-values, or both stacked.
}
\details{
This function visualizes the test results from \code{\link{spectrum_manova}} to help identify
which quantile ranges show significant multigroup separation. The F-statistics reflect the
Hotelling-type test magnitude, while \(-\log_{10}(p)\)-values help assess significance relative
to a chosen cutoff (default 0.05).

If \code{type = "both"}, the function returns a two-panel plot with F-statistics on top and
\(-\log_{10}(p)\)-values below.
}
\examples{
\dontrun{
manova_result <- spectrum_manova(fit)
plot.manova(manova_result, type = "both", p_cutoff = 0.01)
}

}
